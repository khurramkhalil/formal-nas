apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: formal-nas-pvc
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
